// let x = 11;

// console.log(`${x % 2 == 0 ? "genap" : "ganjil"}`);

// HTML FRAGMEN
const mhs = {
  name: "Mahesa Adi Kusuma",
  umur: 19,
  NIM: 20210801132,
  jurusan: "Teknik Informatik",
};

const el = `<div class="mhs">
  <h2>${mhs.name}</h2>
  <span class="NIM">${mhs.name}</span>
</div>`;

console.log(el);
